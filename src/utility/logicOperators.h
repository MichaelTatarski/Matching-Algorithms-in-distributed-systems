#ifndef LOGIC_OPERATORS_H
#define LOGIC_OPERATORS_H

typedef enum
{
    AND,
    OR,

} BooleanOperator;

typedef enum
{
    GREATER_THAN,
    GREATER_EQUAL,
    SMALLER_THAN,
    SMALLER_EQUAL,
    EQUALS

} ConstraintOperator;

#endif // LOGIC_OPERATORS_H